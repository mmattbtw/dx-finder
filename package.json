{
  "name": "dx-finder",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "bunx tsc --noEmit",
    "run": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
